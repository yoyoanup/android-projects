<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.yoyoanup.smartpark">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Homeactivity" />
        <activity android:name=".registration" />
        <activity android:name=".skipreg" />
        <activity android:name=".delhi" />
        <activity android:name=".noidaloc" />
        <activity android:name=".gnoidaloc" />
        <activity android:name=".delhinorthloc" />
        <activity android:name=".delhisouthloc" />
        <activity android:name=".delhieastloc" />
        <activity android:name=".delhiwestloc"></activity>
    </application>

</manifest>